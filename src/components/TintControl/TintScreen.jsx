import TintButtons from './TintControl/TintButtons';

const TintScreen = () => {
	return (
			<div className="TintButtons">
                <TintButtons />
			</div>
	);
};

export default TintScreen;